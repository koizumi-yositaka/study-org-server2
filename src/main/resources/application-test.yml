spring:
  application:
    name: study-org-server
    profiles:
      active: test
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
    schema: src/main/resources/schema.sql
    data: src/main/resources/data.sql

server:
  port: 8085